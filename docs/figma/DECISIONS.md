# قرارات مطابقة Figma

كل قرار هنا مبنيّ على عقد مسمّاة في ملف Figma `xcKbTxQQhUVOFJerTrkrw2`، لا على تفسير. أي مراجعة لاحقة تبدأ من الدليل المذكور.

---

## 1. `Component 16` (العقدة `27:12034`) ليست مصدر تصميم الحقول

**القرار:** لا تُبنى مكوّنات الحقول من `27:12034`. مصدر الحقيقة للحقول هو **إطارات الصفحات** (`91:17109` الحجز ديسكتوب، `141:15629` الحجز موبايل).

**الدليل:**

| ما فُحص | النتيجة |
|---|---|
| حقول الحجز ديسكتوب (`91:17182` Text Input، `91:17189` Email Input، `91:17196` Phone Input) | `FRAME` عادي — **ليست instances** لأي component set. `h=48`, `radius=16`, تعبئة `#FFFFFF`, حدّ `#2B1D14 @ 12%` بوزن `1.33`, حشو `[13.6, 16, 13.6, 16]` |
| حقول الحجز موبايل (`141:15499`, `141:15506`, `141:15513`, `141:15532` Dropdown، `141:15543` Date Picker، `141:15560` Text Area) | نفس الشيء: `FRAME` عادي بارتفاع 48 |
| ذكر `Component 16` في مواصفات `booking-desktop-ar` و`artist-detail-desktop-ar` (583 عقدة معاً) | **صفر** |
| ذكر `Inter` في نفس المواصفات | **صفر**. العائلات المستعملة فعلاً: `Cairo` (108 عقدة)، `Qahwa Arabic` (3)، `DM Mono` (4)، `SF Pro` (1) |
| الرماديات `#E1E1E1 #D2D2D2 #9E9E9E #909090 #424242` ولونا `#4CAF50` / `#F44336` في نفس المواصفات | **صفر** |

**الخلاصة:** `27:12034` هي component set من قالب Material/Inter جاهز، مركون في صفحة design system وغير مستعمَل في أي شاشة منتج. رماديّاته (`Foundation /gradescale/*` = سلّم Material الرمادي) وخطّه (Inter) وارتفاعاته (44px) وظلّه (`drop-shadow 0 4 6`) **ليست تصميم الموقع**. نسخها إلى الكود كان سيُبعد الكود عن الشاشات الحقيقية لا يقرّبه إليها.

**الأثر على الكود:** `src/components/ui/Input.tsx` الحالي (`h-[48px]`, `rounded-input`=16, `border-brand-espresso-subtle`=`rgba(43,29,20,0.12)`, `px-4 py-3.5`) **يطابق إطارات الصفحات أصلاً**. العمل المتبقّي هو توحيد التطبيقات الثلاث على هذا التعريف، لا إعادة بنائها على مصفوفة الـ36 تنويعة.

---

## 2. سلّم الرمادي: `gradscale` يبقى كما هو

**القرار:** لا يُضاف `--color-gradescale-*`. سلّم `globals.css` الحالي صحيح.

**الدليل:** إطار `colors` في design system (`27:9881`) — وهو توثيق البالتة الرسمي — يعنون شرائحه `gradscale-50 … gradscale-900` ويحمل بالضبط قيم المستودع:

```
#ECECEC #C4C4C4 #A7A7A7 #7F7F7F #666666 #404040 #3A3A3A #2D2D2D #232323 #1B1B1B
```

وكذلك `secondary-50…900` و`primary-50…900` في الإطار نفسه تطابق `globals.css` قيمةً بقيمة.

أما مجموعة المتغيّرات `Foundation /gradescale/*` (بحرف e) فتحمل قيم Material ولا يشير إليها إلا `27:12034` وجيرانه في صفحة design system. `Foundation /neutral/*` تكرار لها (`neutral-100` = `#E1E1E1`, `neutral-900` = `#424242`).

**تعارض داخلي في ملف Figma نفسه، مسجَّل للعلم:** المتغيّر `Foundation /primary/primary-100` يعيد `#EDC6B7` داخل `27:12015` لكن `#BBEDF4` (أزرق سماوي) داخل `27:12129`. القيمة المعتمدة هي `#EDC6B7` لأنها تطابق إطار `colors` و`globals.css:37`.

---

## 3. الخطوط: Cairo/Qahwa، وInter لا يُضاف

**القرار:** لا يُضاف Inter إلى المشروع. تبقى `--font-sans: Cairo` و`--font-display: "Qahwa Arabic"`.

**الدليل:** `Qahwa Arabic` تظهر فعلاً في 3 عقد نصية في `artist-detail-desktop-ar` — فهي خط حقيقي في التصميم لا افتراضاً في التوثيق. Inter يظهر في صفحة design system فقط (`Desktop/*` styles)، وSF Pro في أنماط `Mobile/*` وفي عقدة واحدة داخل كل إطار صفحة.

**ما يُؤخذ من design system:** مقاييس السلّم فقط (الحجم، ارتفاع السطر، الوزن) حيث تطابق ما تستعمله إطارات الصفحات — لا عائلة الخط.

---

## 4. سلّم المسافات = سلّم `grid N` في Figma

**الدليل:** `get_variable_defs` على `27:12129` («gride and space») يعيد:

```
grid 8, 16, 24, 32, 40, 56, 64, 72, 84, 88, 96, 104, 112
```

**تصحيح (كان هذا القسم مخطئاً):** قيل سابقاً إن هذا «مطابق تماماً» لـ`FIGMA_TOKENS.spacing`. ليس كذلك. سلّم Figma فيه **ثلاث عشرة** درجة ويقفز من 40 إلى 56 — **لا وجود لـ48**. وخريطة `1..14` القديمة في `tokens.ts` كانت تحتوي 48 في الموضع السادس، أي أنها **استنبطت درجة لا يعرّفها التصميم**، ثم قرأ هذا القسم التطابق العددي (14 مفتاحاً) كأنه تطابق قيمي.

**المنفَّذ:** أُعيد ترقيم `FIGMA_TOKENS.spacing` بالقيمة لا بالترتيب (`8: "8px"` … `112: "112px"`)، لأن هذا هو اسم المتغيّر في Figma حرفياً (`grid 8`)، ولأن المفاتيح الترتيبية هي ما أوحى بسلّم متّصل فأنتج الـ48. وحُدّث `tests/ui-foundation.test.ts` ليثبّت غياب 48 صراحةً.

**النشر:** بأسماء مسمّاة `--spacing-grid-8 … --spacing-grid-112`، **لا** كـ`--spacing-1 … --spacing-13`: في Tailwind v4 ذلك يتجاوز سلّم المسافات الديناميكي فيصير `p-1`/`gap-3`/`p-4` يعني 8px/24px/32px بدل 4px/16px — إعادة قياس صامتة للمشروع كله. **تحقُّق فعلي:** الـCSS المُصرَّف لا يزال يحمل `--spacing: .25rem`، ويحرس `tests/token-parity.test.ts` ذلك بـ`doesNotMatch(/--spacing-\d+\s*:/)`.

**الحاويات:** `--container-desktop: 1280px` و`--container-mobile: 390px`. و1280 مؤكَّدة مرّتين: شبكة design system، ثم الحاوية الداخلية للمُذيّل نفسه (1280 داخل 1454). وقد استُهلكت فعلاً في `Footer.tsx` (`max-w-desktop`)، والقياس يعطي `x=73 w=1280` — أي `−14 + (1454−1280)/2` بالضبط.

كذلك يعيد `27:12015` المتغيّر `12 RC` = `12` — وهو نصف قطر الأزرار (`--radius-button: 12px`، `globals.css:95`)، فيُعتبر مؤكَّداً.

---

## 5. الهيكل: الديسكتوب مطابق أصلاً، والموبايل كان مُختلقاً

### 5.أ شريط التنقّل الديسكتوب ومُذيّل الصفحة — **صحيحان، لا يُلمَسان**

قياس مستقلّ لصور المرجع الثمانية (صفّ أول من شريط `#F2EEE0`، وأول صفّ `#2B1D14` من الأسفل) أعطى:

| الإطار | `nav_top` مقاساً | جدول `NAVBAR_TOP` في الكود |
|---|---:|---:|
| home-desktop | 33 | 33 |
| academy-desktop | 57 | 57 |
| events-desktop | 40 | 40 |
| booking-desktop | 50 | 50 (افتراضي) |
| news-desktop | 50 | 50 (افتراضي) |
| artists-desktop | 50 | 50 (افتراضي) |
| artist-detail-desktop | 40 | 40 (نمط `/artists/.+`) |

سبعة من سبعة. وكذلك المُذيّل: أعلاه = `canvas − 384.67` في كل إطار، أي أنه مرسى إلى الأسفل بارتفاع ثابت.

وقياس التصيير الفعلي لـ`news-desktop` أعطى الشريط `y=50 h=85 x=159 w=1123` مقابل عقدة Figma `94:18677` (1123×85، ومركزها `(1440−1123)/2 = 158.5`)، والمُذيّل `h=385` مقابل `94:18553` بارتفاع `384.667`. والـ`translateX(-7px)` في `Footer.tsx:38` يعيد إنتاج هندسة Figma بدقّة: المُذيّل هناك `1454` عرضاً عند `x=-14`، فحاويته الداخلية `1280` تبدأ عند `-14 + (1454−1280)/2 = 73`، وهو ما يصيّره الكود (`(1440−1280)/2 = 80`, ثم `−7`).

**الخلاصة:** انحراف `+24px` في `news-desktop` **ليس في الهيكل**. Figma يضع المُذيّل عند `y=1568` ونحن نصيّره عند `1592`، أي أن `main` يزيد 24px — والخطأ في جسم الصفحة (المرحلة D).

### 5.ب شريط التنقّل الموبايل — أُعيد بناؤه على المواصفة

`Component 17/Navigation` (`139:12348`، 369.73×56) يحتوي **طفلين فقط**:

| العقدة | الصندوق |
|---|---|
| `139:12338` `basil:menu-outline` | 24×24 عند `(20, 16)` |
| `139:12341` `Image (Andalusia)` | 104×32 عند `(245.73, 12)` — حافّته اليمنى على 20px من الطرف |

وقياس `home-mobile.png` 1:1: الحبّة `x=10..378`, `y=44..99` → `370×56` عند `(10,44)`, نصف قطر ≈ **20** (الأبيض يصل الحافّة عند `dy=19`), تعبئة `#FFFFFF`, و**بلا ظلّ** (الصفّ `y=100` هو الهيرو نفسه بلا تدرّج). أيقونة القائمة ثلاثة خطوط بسماكة 2 ومسافة 5، بلون **`#404040`** = `gradscale-500`.

**ما كان في الكود ولا وجود له في Figma:** شريط `fixed top-0` ممتدّ بعرض الشاشة بخلفية `bg-white/95 backdrop-blur-md` وحدّ سفلي، ومربّع `MusicIcon`, ونصّ `site("brand")` ظاهر، وزرّ «أحجز الآن». الأربعة حُذفت. زرّ الحجز ومبدّل اللغة موجودان في الـdrawer أصلاً.

**الاختبارات:** `tests/public-shell.test.ts` و`tests/empirical-m1-challenge.test.ts` كانا يثبّتان الشريط المُختلق (`fixed top-0 start-0 end-0`, `px-4 sm:px-6`, `nav.bookNow`). حُدّثا ليثبّتا المواصفة المقاسة بنفس الصرامة، مع `doesNotMatch` على الزرّ والمبدّل كي لا يعود التصميم المُختلق تسلّلاً. واستُبدل `\d{3,}` في فحص العرض بتحليل رقمي فعلي للقيمة مقابل صندوق محتوى الحبّة (330px) — الصيغة القديمة لم تكن تميّز `w-[104px]` المشروع من `w-[400px]`.

**معلَّق:** الإزاحة `44px` مقاسة من إطار الرئيسية وحده (هو الوحيد الذي يملك صورة مرجع بين إطارات الموبايل). الديسكتوب يتغيّر بين 33 و57 حسب الشاشة، فقد تتغيّر الستّة الباقية؛ تُؤكَّد في المرحلة E.

### 5.ج إهمال `priority` في next/image

`node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md:293` — `priority` مهجور في Next 16 لصالح `preload`، والدليل يوصي `loading="eager"` أو `fetchPriority="high"` في معظم الحالات. طُبِّق على شعاري الشريطين (ليسا عنصر LCP). ولا يزال `priority` مستعملاً في 9 مكوّنات أخرى (`NewsHero`, `ArtistCard`, `EventCard`, `FeaturedEventBanner`, `ArtistTile`…) — تُعالَج في مراحلها، وبعضها **هيرو فعلاً** فيستحقّ `preload` لا `loading="eager"`.

---

## 6. الإطارات الموبايل الغامضة الأربعة

محسومة في `docs/figma/frame-overrides.json` مع دليل كل واحدة. الخلاصة: `141:15629` = الحجز، `141:15199` = الأخبار، وتوأماهما الإنجليزيان `144:20146` = الحجز، `144:20463` = الأخبار (مشتقّان من الترتيب وتطابق الارتفاع، ويُعاد تأكيدهما بنيوياً في مرحلة E4).
