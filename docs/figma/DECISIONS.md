# قرارات مطابقة Figma

كل قرار هنا مبنيّ على عقد مسمّاة في ملف Figma `xcKbTxQQhUVOFJerTrkrw2`، لا على تفسير. أي مراجعة لاحقة تبدأ من الدليل المذكور.

---

## 1. `Component 16` (العقدة `27:12034`) ليست مصدر تصميم الحقول

**القرار:** لا تُبنى مكوّنات الحقول من `27:12034`. مصدر الحقيقة للحقول هو **إطارات الصفحات** (`91:17109` الحجز ديسكتوب، `141:15629` الحجز موبايل).

**الدليل:**

| ما فُحص | النتيجة |
|---|---|
| حقول الحجز ديسكتوب (`91:17182` Text Input، `91:17189` Email Input، `91:17196` Phone Input) | `FRAME` عادي — **ليست instances** لأي component set. `h=48`, `radius=16`, تعبئة `#FFFFFF`, حدّ `#2B1D14 @ 12%` بوزن `1.33`, حشو `[13.6, 16, 13.6, 16]` |
| حقول الحجز موبايل (`141:15499`, `141:15506`, `141:15513`, `141:15532` Dropdown، `141:15543` Date Picker، `141:15560` Text Area) | نفس الشيء: `FRAME` عادي بارتفاع 48 |
| ذكر `Component 16` في مواصفات `booking-desktop-ar` و`artist-detail-desktop-ar` (583 عقدة معاً) | **صفر** |
| ذكر `Inter` في نفس المواصفات | **صفر**. العائلات المستعملة فعلاً: `Cairo` (108 عقدة)، `Qahwa Arabic` (3)، `DM Mono` (4)، `SF Pro` (1) |
| الرماديات `#E1E1E1 #D2D2D2 #9E9E9E #909090 #424242` ولونا `#4CAF50` / `#F44336` في نفس المواصفات | **صفر** |

**الخلاصة:** `27:12034` هي component set من قالب Material/Inter جاهز، مركون في صفحة design system وغير مستعمَل في أي شاشة منتج. رماديّاته (`Foundation /gradescale/*` = سلّم Material الرمادي) وخطّه (Inter) وارتفاعاته (44px) وظلّه (`drop-shadow 0 4 6`) **ليست تصميم الموقع**. نسخها إلى الكود كان سيُبعد الكود عن الشاشات الحقيقية لا يقرّبه إليها.

**الأثر على الكود:** `src/components/ui/Input.tsx` الحالي (`h-[48px]`, `rounded-input`=16, `border-brand-espresso-subtle`=`rgba(43,29,20,0.12)`, `px-4 py-3.5`) **يطابق إطارات الصفحات أصلاً**. العمل المتبقّي هو توحيد التطبيقات الثلاث على هذا التعريف، لا إعادة بنائها على مصفوفة الـ36 تنويعة.

---

## 2. سلّم الرمادي: `gradscale` يبقى كما هو

**القرار:** لا يُضاف `--color-gradescale-*`. سلّم `globals.css` الحالي صحيح.

**الدليل:** إطار `colors` في design system (`27:9881`) — وهو توثيق البالتة الرسمي — يعنون شرائحه `gradscale-50 … gradscale-900` ويحمل بالضبط قيم المستودع:

```
#ECECEC #C4C4C4 #A7A7A7 #7F7F7F #666666 #404040 #3A3A3A #2D2D2D #232323 #1B1B1B
```

وكذلك `secondary-50…900` و`primary-50…900` في الإطار نفسه تطابق `globals.css` قيمةً بقيمة.

أما مجموعة المتغيّرات `Foundation /gradescale/*` (بحرف e) فتحمل قيم Material ولا يشير إليها إلا `27:12034` وجيرانه في صفحة design system. `Foundation /neutral/*` تكرار لها (`neutral-100` = `#E1E1E1`, `neutral-900` = `#424242`).

**تعارض داخلي في ملف Figma نفسه، مسجَّل للعلم:** المتغيّر `Foundation /primary/primary-100` يعيد `#EDC6B7` داخل `27:12015` لكن `#BBEDF4` (أزرق سماوي) داخل `27:12129`. القيمة المعتمدة هي `#EDC6B7` لأنها تطابق إطار `colors` و`globals.css:37`.

---

## 3. الخطوط: Cairo/Qahwa، وInter لا يُضاف

**القرار:** لا يُضاف Inter إلى المشروع. تبقى `--font-sans: Cairo` و`--font-display: "Qahwa Arabic"`.

**الدليل:** `Qahwa Arabic` تظهر فعلاً في 3 عقد نصية في `artist-detail-desktop-ar` — فهي خط حقيقي في التصميم لا افتراضاً في التوثيق. Inter يظهر في صفحة design system فقط (`Desktop/*` styles)، وSF Pro في أنماط `Mobile/*` وفي عقدة واحدة داخل كل إطار صفحة.

**ما يُؤخذ من design system:** مقاييس السلّم فقط (الحجم، ارتفاع السطر، الوزن) حيث تطابق ما تستعمله إطارات الصفحات — لا عائلة الخط.

---

## 4. سلّم المسافات = سلّم `grid N` في Figma

**الدليل:** `get_variable_defs` على `27:12129` («gride and space») يعيد:

```
grid 8, 16, 24, 32, 40, 56, 64, 72, 84, 88, 96, 104, 112
```

**تصحيح (كان هذا القسم مخطئاً):** قيل سابقاً إن هذا «مطابق تماماً» لـ`FIGMA_TOKENS.spacing`. ليس كذلك. سلّم Figma فيه **ثلاث عشرة** درجة ويقفز من 40 إلى 56 — **لا وجود لـ48**. وخريطة `1..14` القديمة في `tokens.ts` كانت تحتوي 48 في الموضع السادس، أي أنها **استنبطت درجة لا يعرّفها التصميم**، ثم قرأ هذا القسم التطابق العددي (14 مفتاحاً) كأنه تطابق قيمي.

**المنفَّذ:** أُعيد ترقيم `FIGMA_TOKENS.spacing` بالقيمة لا بالترتيب (`8: "8px"` … `112: "112px"`)، لأن هذا هو اسم المتغيّر في Figma حرفياً (`grid 8`)، ولأن المفاتيح الترتيبية هي ما أوحى بسلّم متّصل فأنتج الـ48. وحُدّث `tests/ui-foundation.test.ts` ليثبّت غياب 48 صراحةً.

**النشر:** بأسماء مسمّاة `--spacing-grid-8 … --spacing-grid-112`، **لا** كـ`--spacing-1 … --spacing-13`: في Tailwind v4 ذلك يتجاوز سلّم المسافات الديناميكي فيصير `p-1`/`gap-3`/`p-4` يعني 8px/24px/32px بدل 4px/16px — إعادة قياس صامتة للمشروع كله. **تحقُّق فعلي:** الـCSS المُصرَّف لا يزال يحمل `--spacing: .25rem`، ويحرس `tests/token-parity.test.ts` ذلك بـ`doesNotMatch(/--spacing-\d+\s*:/)`.

**الحاويات:** `--container-desktop: 1280px` و`--container-mobile: 390px`. و1280 مؤكَّدة مرّتين: شبكة design system، ثم الحاوية الداخلية للمُذيّل نفسه (1280 داخل 1454). وقد استُهلكت فعلاً في `Footer.tsx` (`max-w-desktop`)، والقياس يعطي `x=73 w=1280` — أي `−14 + (1454−1280)/2` بالضبط.

كذلك يعيد `27:12015` المتغيّر `12 RC` = `12` — وهو نصف قطر الأزرار (`--radius-button: 12px`، `globals.css:95`)، فيُعتبر مؤكَّداً.

---

## 5. الهيكل: الديسكتوب مطابق أصلاً، والموبايل كان مُختلقاً

### 5.أ شريط التنقّل الديسكتوب ومُذيّل الصفحة — **صحيحان، لا يُلمَسان**

قياس مستقلّ لصور المرجع الثمانية (صفّ أول من شريط `#F2EEE0`، وأول صفّ `#2B1D14` من الأسفل) أعطى:

| الإطار | `nav_top` مقاساً | جدول `NAVBAR_TOP` في الكود |
|---|---:|---:|
| home-desktop | 33 | 33 |
| academy-desktop | 57 | 57 |
| events-desktop | 40 | 40 |
| booking-desktop | 50 | 50 (افتراضي) |
| news-desktop | 50 | 50 (افتراضي) |
| artists-desktop | 50 | 50 (افتراضي) |
| artist-detail-desktop | 40 | 40 (نمط `/artists/.+`) |

سبعة من سبعة. وكذلك المُذيّل: أعلاه = `canvas − 384.67` في كل إطار، أي أنه مرسى إلى الأسفل بارتفاع ثابت.

وقياس التصيير الفعلي لـ`news-desktop` أعطى الشريط `y=50 h=85 x=159 w=1123` مقابل عقدة Figma `94:18677` (1123×85، ومركزها `(1440−1123)/2 = 158.5`)، والمُذيّل `h=385` مقابل `94:18553` بارتفاع `384.667`. والـ`translateX(-7px)` في `Footer.tsx:38` يعيد إنتاج هندسة Figma بدقّة: المُذيّل هناك `1454` عرضاً عند `x=-14`، فحاويته الداخلية `1280` تبدأ عند `-14 + (1454−1280)/2 = 73`، وهو ما يصيّره الكود (`(1440−1280)/2 = 80`, ثم `−7`).

**الخلاصة:** انحراف `+24px` في `news-desktop` **ليس في الهيكل**. Figma يضع المُذيّل عند `y=1568` ونحن نصيّره عند `1592`، أي أن `main` يزيد 24px — والخطأ في جسم الصفحة (المرحلة D).

### 5.ب شريط التنقّل الموبايل — أُعيد بناؤه على المواصفة

`Component 17/Navigation` (`139:12348`، 369.73×56) يحتوي **طفلين فقط**:

| العقدة | الصندوق |
|---|---|
| `139:12338` `basil:menu-outline` | 24×24 عند `(20, 16)` |
| `139:12341` `Image (Andalusia)` | 104×32 عند `(245.73, 12)` — حافّته اليمنى على 20px من الطرف |

وقياس `home-mobile.png` 1:1: الحبّة `x=10..378`, `y=44..99` → `370×56` عند `(10,44)`, نصف قطر ≈ **20** (الأبيض يصل الحافّة عند `dy=19`), تعبئة `#FFFFFF`, و**بلا ظلّ** (الصفّ `y=100` هو الهيرو نفسه بلا تدرّج). أيقونة القائمة ثلاثة خطوط بسماكة 2 ومسافة 5، بلون **`#404040`** = `gradscale-500`.

**ما كان في الكود ولا وجود له في Figma:** شريط `fixed top-0` ممتدّ بعرض الشاشة بخلفية `bg-white/95 backdrop-blur-md` وحدّ سفلي، ومربّع `MusicIcon`, ونصّ `site("brand")` ظاهر، وزرّ «أحجز الآن». الأربعة حُذفت. زرّ الحجز ومبدّل اللغة موجودان في الـdrawer أصلاً.

**الاختبارات:** `tests/public-shell.test.ts` و`tests/empirical-m1-challenge.test.ts` كانا يثبّتان الشريط المُختلق (`fixed top-0 start-0 end-0`, `px-4 sm:px-6`, `nav.bookNow`). حُدّثا ليثبّتا المواصفة المقاسة بنفس الصرامة، مع `doesNotMatch` على الزرّ والمبدّل كي لا يعود التصميم المُختلق تسلّلاً. واستُبدل `\d{3,}` في فحص العرض بتحليل رقمي فعلي للقيمة مقابل صندوق محتوى الحبّة (330px) — الصيغة القديمة لم تكن تميّز `w-[104px]` المشروع من `w-[400px]`.

**معلَّق:** الإزاحة `44px` مقاسة من إطار الرئيسية وحده (هو الوحيد الذي يملك صورة مرجع بين إطارات الموبايل). الديسكتوب يتغيّر بين 33 و57 حسب الشاشة، فقد تتغيّر الستّة الباقية؛ تُؤكَّد في المرحلة E.

### 5.ج إهمال `priority` في next/image

`node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md:293` — `priority` مهجور في Next 16 لصالح `preload`، والدليل يوصي `loading="eager"` أو `fetchPriority="high"` في معظم الحالات. طُبِّق على شعاري الشريطين (ليسا عنصر LCP). ولا يزال `priority` مستعملاً في 9 مكوّنات أخرى (`NewsHero`, `ArtistCard`, `EventCard`, `FeaturedEventBanner`, `ArtistTile`…) — تُعالَج في مراحلها، وبعضها **هيرو فعلاً** فيستحقّ `preload` لا `loading="eager"`.

---

## 6. أصول Figma محجوبة فعلاً — أُكِّد بثلاث طرق مستقلّة

خادم Figma MCP يعمل ويعطي الهندسة والمتغيّرات و**صوراً للعرض**، لكنه **لا يعطي بايتات الأصول**:

| الطريق | النتيجة |
|---|---|
| `curl` على رابط أصل من `download_assets` | `curl: (56) CONNECT tunnel failed, response 403` |
| `curl "$HTTPS_PROXY/__agentproxy/status"` | `www.figma.com:443` → `connect_rejected` / «gateway answered 403 to CONNECT (policy denial)» |
| `get_screenshot` مع `enableBase64Response: true` | تعود الصورة كـ**كتلة صورة معروضة**، لا كنصّ base64 يمكن فكّه وكتابته على القرص |

`download_assets` يعيد روابط على `www.figma.com/api/mcp/asset/...` — أي على المضيف المحجوب نفسه. فالنتيجة العملية:

- **متاح:** كل الهندسة (`get_metadata`)، كل المتغيّرات (`get_variable_defs`)، و**الفحص البصري** لأي عقدة (`get_screenshot`) — وهذا مفيد فعلاً للتحقّق من التصميم.
- **غير متاح:** صور مرجعية جديدة للإطارات العشرين، وصور الهيرو الفوتوغرافية بدقّة البكسل.

**الأثر المقاس:** `news-desktop` صار مطابقاً هندسياً تماماً (1953/1953)، ومع ذلك يبقى فرقه 17.6٪، موزّعاً:

| النطاق | الفرق |
|---|---:|
| شريط الهيرو 0–668 | **46.8٪** |
| الفجوة 668–849 | 1.5٪ |
| العنوان | 5.6٪ |
| صفّ البطاقات 921–1345 | **2.3٪** |
| الذيل | 0.0٪ |
| المُذيّل | 4.2٪ |

أي أن الفرق المتبقّي **كلّه تقريباً في صورة الهيرو المفقودة**، لا في الهندسة. العلاج إداري: السماح بـ`www.figma.com` يفتح هذا كلّه.

---

## 7. تعداد الـ28 إطاراً: الديسكتوب نجح هندسياً، والموبايل بناء

قياس `measure-layout.mjs` للـ28 كلّها (`scrollHeight` مقابل اللوحة):

| المجموعة | النتيجة |
|---|---|
| **14 إطار ديسكتوب** (7 عربي + 7 إنجليزي) | **كلّها ضمن ±1px** — 0, −1, 0, 0, 0, +1, +1 لكل لغة. داخل حدّ G1 (±8px / 0.5٪) بفارق مريح. والإنجليزي يطابق العربي رقماً برقم، فالانعكاس نظيف. |
| **14 إطار موبايل** | **كلّها فاشلة** |

تفصيل الموبايل (عربي):

| الإطار | اللوحة | المقاس | الفرق |
|---|---:|---:|---:|
| artists-mobile | 2889 | 6008 | **+3119** |
| artist-detail-mobile | 3658 | 5353 | +1695 |
| news-mobile | 3037 | 3601 | +564 |
| events-mobile | 2889 | 3022 | +133 |
| home-mobile | 6528 | 6591 | +63 |
| academy-mobile | 3891 | 3761 | −130 |
| booking-mobile | 3434 | 3124 | −310 |

هذا يثبّت فرضية الخطّة: **الموبايل بناء لا تعديل**. ومثاله الأوضح `artists-mobile`.

### 7.أ `artists-mobile`: التصميم شريطان أفقيان، والكود رصّة عمودية

`141:15048 Frame 40` عند `(-17, 809)` بقياس `387×900` يحتوي صفَّين (`Frame 19` عند y=0 و`Frame 21` عند y=458، كلّ منهما `355×442`). وداخل كلّ صفّ **أربع** بطاقات `Artists` بعرض 296 عند `x = -869, -563, -257, 49` — أي خطوة 306 (فجوة 10)، وواحدة فقط داخل الإطار والثلاث الباقية منزلقة خارجه يساراً.

هذا **كاروسيل أفقي RTL** مبدوء من أوّله: البطاقة المرئية عند `49..345` من 355، أي ملتصقة بالحافّة اليمنى بهامش 10، وما قبلها يمتدّ خارج الشاشة يساراً. الكود يصيّر كل الفنانين رصّة عمودية بعرض كامل — ومن هنا الـ`+3072` في جسم الصفحة.

**البطاقة:** `296×422`؛ حاوية صورة عند `y=-26` بارتفاع 340 تحمل صورة `296×363` (تُقصّ)، ثم حاوية نصّ عند `y=314` بارتفاع 134: eyebrow 20، `Heading 3` 31.8، سطر مكان 22.4.

### 7.ب مُذيّل الموبايل: 882 مقابل 929 مقاساً — يشترك فيه الـ14 كلّها

نسخة المُذيّل `140:14746` هي `390×882` عند `y=2007`، و`2007 + 882 = 2889` = اللوحة بالضبط، فهو مرسى إلى الأسفل بارتفاع ثابت 882. ونحن نصيّر **929**، أي `+47` في كل إطار موبايل.

إيقاعه العمودي من Figma (نسبةً إلى أعلى المُذيّل):

| الكتلة | من | الارتفاع |
|---|---:|---:|
| حشو أعلى | — | 21 |
| العلامة (شعار `210.5×85.5` + الرسالة + الشعار النصّي) | 21 | 179.5 |
| فجوة | — | 46.5 |
| «استكشف» + روابطه | 247 | 190 |
| «تواصل» | 437 | 137 |
| فجوة | — | 29 |
| نداء الحجز (عنوان 24 + نصّ 73.4 + زرّ `150×48`) | 603 | 190 |
| الشريط السفلي | 813 | 42.67 |
| حشو أسفل | — | 26.33 |

المجموع 882. ولاحظ أن «استكشف» و«تواصل» كلتاهما عند `x=222` بعرض 156 — **مرصوصتان عمودياً لا جنباً إلى جنب**.

**نُفِّذ.** المُذيّل الآن `882` بالضبط، وكل كتلة داخله على قيمة Figma:

| الكتلة | Figma (y, h, x, w) | المقاس | الفرق |
|---|---|---|---|
| العلامة | 21, 179.5, 44, 345 | 21, 180, 44, 345 | +0.5 في الارتفاع |
| «استكشف» | 247, 190, 222, 156 | 247, 190, 222, 156 | 0 |
| «تواصل» | 437, 137, 222, 156 | 437, 137, 222, 156 | 0 |
| نداء الحجز | 603, 190, 44, 321 | 603, 190, 44, 321 | 0 |
| الشريط السفلي | 813, 42.67, 24, 342 | 813, 43, 24, 342 | +0.33 في الارتفاع |

ثلاث نقاط حسمها القياس ولم تكن في الوصف الأوّل:

1. **الإزاحات الأفقية ثلاث قيم لا واحدة** — الحوافّ اليمنى للكتل هي 389 و378 و378 و365، أي إزاحات 1 و12 و12 و25 من بداية السطر. واثنتان منها (1 و12) **أصغر من حشو المُذيّل نفسه (24)**، فالعمود مضطرّ أن يخرج من الحشو: `-mx-6` على العمود ثم `ms-*` لكل كتلة. والشريط السفلي وحده يجلس على الـ24 (`x=24 w=342`) — وهو ما يؤكّد `px-6` أصلاً.
2. **الشريط السفلي صفّ واحد على الموبايل أيضاً** — `136:7847` هو `342×42.667` يحمل النصّين جنباً إلى جنب (125 و205 بفجوة 12، ملتصقين بحافّته السفلى). الكود كان يرصّهما عمودياً تحت `sm:` فأنتج 74 بدل 42.667. صار `flex-row-reverse` بلا بوابة، و`items-end`، و`mt-5` (20) بدل 56.
3. **صناديق الأعمدة مثبّتة الارتفاع، والفراغ داخلها هو الفاصل** — «استكشف» 190 بينما عنوانه وروابطه تشغل 152.4؛ الفائض 37.6 هو ما يفصلها عن «تواصل» التي تبدأ ملتصقة عند 437. فلا تُبنى الفواصل هنا بـ`gap` بل بارتفاع مُعلَن.

الأثر على الـ14 إطار موبايل (قبل ← بعد، الفرق عن اللوحة):

| الإطار | قبل | بعد |
|---|---:|---:|
| home-mobile | +63 | **+16** |
| events-mobile | +133 | +87 |
| news-mobile | +564 | +517 |
| artist-detail-mobile | +1695 | +1648 |
| artists-mobile | +3119 | +3072 |
| academy-mobile | −130 | −177 |
| booking-mobile | −310 | −356 |

الإنجليزي تحرّك بالمثل. ولاحظ `academy` و`booking`: كانا **أقصر** من اللوحة، فحذف 47 زادهما قِصَراً — وهذا صحيح لا انحدار: عجزهما في جسم الصفحة لا في المُذيّل، ويُبنى في دوره.

والديسكتوب لم يتحرّك: المُذيّل 385 في السبعة كلّها وارتفاعات الصفحات كما هي (0, 0, +1, −1, 0, +1, 0).

---

## 8. الإطارات الموبايل الغامضة الأربعة

محسومة في `docs/figma/frame-overrides.json` مع دليل كل واحدة. الخلاصة: `141:15629` = الحجز، `141:15199` = الأخبار، وتوأماهما الإنجليزيان `144:20146` = الحجز، `144:20463` = الأخبار (مشتقّان من الترتيب وتطابق الارتفاع، ويُعاد تأكيدهما بنيوياً في مرحلة E4).


---

## 9. الإطارات الإنجليزية: الديسكتوب مترجم والموبايل لا

فحصٌ كان لازماً قبل تبويب الـ14 إطاراً الإنجليزية:

| المجموعة | المدى | النصّ |
|---|---|---|
| ديسكتوب إنجليزي | `136:*` | **إنجليزي فعلاً.** لقطة `136:3049` (news-desktop-en) تقرأ "Opening of the Annual Art Exhibition in Andalusia" و"latest news" و"Explore" و"Contact" و"Your next concert starts here." |
| موبايل إنجليزي | `144:*` | **عربي.** الإطار `144:19902` — الذي يربطه المانيفست بـ`events-mobile-en` — اسمه «الفعاليات» وكلّ نصّ فيه عربي: «مهرجان الربيع الموسيقي»، «احجز»، «مارس». ومُذيّله `144:19996` هو نفسه `390×882`. |

أي أن مجموعة الموبايل الإنجليزية **نُسخت ولم تُترجَم**. وهذا يغيّر معنى البوابة لسبعة إطارات: هندستها المحكومة بطول النصّ لا مرجع لها، لأن اللوحة تحمل مقاييس النصّ العربي.

أول أثر مقاس لذلك: الشريط السفلي للمُذيّل. في العربية «موسيقى · ثقافة · قدرة» (125) و«© أندلسيا…» (205) = 330 + فجوة 12 = 342 بالضبط، أي أن التصميم يملأ الصندوق تماماً. وفي الإنجليزية سلسلتانا أطول فتلتفّ كلتاهما إلى سطرين، فيصير المُذيّل 900 بدل 882 — **+18 في سبعة إطارات موبايل إنجليزية**. لا يمكن حسمه من Figma لأن Figma لا يحمل تخطيطاً إنجليزياً للموبايل؛ يحتاج قراراً تصميمياً.

وبالمناسبة: لقطة `136:3049` تقرأ الشعار النصّي الإنجليزي **"MUSIC · CULTURE · ABILITY"** بينما `src/messages/en.json:43` يقول `"MUSIC · CULTURE · CRAFT"`. يُثبَّت على نصّ Figma في المرحلة G (ويحتاج تأكيداً بتكبير أعلى قبل التغيير).

## 10. `tests/empirical-m2-upper-stages.test.ts` خارج المُشغّل

الملف موجود في المستودع لكنه **ليس** في قائمة `npm test` في `package.json`. ومحتواه سابق لعمل Figma: يؤكّد `aspect-[4/5]` و`rounded-card` على `ArtistCard` — والبطاقة المؤكَّدة من الإطارات هي `296×422` بنصف قطر 16 وبلا نسبة أبعاد. فهو مصدر رابع «يبدو معقولاً» ويقول عكس الحقيقة المقاسة. لم يُحذف ولم يُوصَل: أيّهما قرار يخصّ صاحب المستودع (الوصل يُفشل البناء، والحذف يُزيل اختبارات).

## 11. `events-mobile`: كلّ قسم مطابق، والارتفاع الكلّي غير قابل للمطابقة

`144:19902` أقسامه كلّها مُنتَجة بالضبط الآن:

| القسم | Figma | المقاس |
|---|---|---|
| الهيرو | 0..678 | 0..678 |
| شريط التصفية (`Component 8`) | y=716, 376×54, x=14 | y=716, 376×54, x=14 |
| قائمة الفعاليات (`Frame 39`) | y=808, x=0, w=390.33 | y=808, x=0, w=390 |
| البطاقة (`144:19913`) | 370.33×150.33 at x=10 | 370×150 at x=10 |
| خانة المميّز (`144:19982`) | 349×586 at x=21 | 349×586 at x=21 |
| المُذيّل | y=2007 | y=2007 |

ويبقى **+321**، وهو بالضبط صفّان زائدان (`2 × 160.33 = 320.67`). والسبب ليس تخطيطاً:

1. `Frame 39` يحمل ثلاثة صفوف، و**الثالث تكرار للأول** («مهرجان الربيع الموسيقي» مرّتين). فالعدد حشوٌ في ماكيت لا قراراً تصميمياً.
2. وإطار الديسكتوب يرسم **خمسة** صفوف — والدليل أنّ `events-desktop` يطابق لوحته 2290 بفرق 0 مع خمس بياناتنا الثابتة؛ لو كان التصميم ثلاثة لكنّا +332.

فـ491 الذي يقيسه ذلك الصندوق يشفّر عدد صفوف الماكيت. لذلك **يُبوَّب هذا الإطار قسماً بقسم لا بارتفاعه الكلّي**، وهذا مثبَّت في `tests/events-page.test.ts`.

ولاحظ أن `144:19982` صندوق فارغ تماماً (`get_metadata` عليه مباشرةً يعيده بلا أبناء): الإطار **يحجز** الخانة ولا يصمّم داخلها. فأُعيد إنتاج الحجز ومُلئ بالبانر القائم.

## 12. `news-mobile`: كلّ ما يحدّده الإطار مطابق، والباقي صندوق غير مصمَّم + 8 من الإطار نفسه

`141:15199` (لوحة `390×3037`). كلّ رقم يحدّده الإطار مُنتَج الآن بالضبط:

| الكتلة | Figma | المقاس |
|---|---|---|
| `Frame 34` | y=715, x=24 | y=1038 (أي 47 تحت الهيرو)، x=24 w=342 |
| `Heading 2` | 48، والسطر 40 على بعد 8 | الكتلة 48، السطر 40 على بعد 8 |
| الشبكة | تبدأ 24 تحت العنوان | 24 |
| البطاقة (`141:15080`) | 342×422.5، خطوة 446.5 | 342×423، خطوة 447 |

والمتبقّي **+331** يتفكّك بالكامل إلى سببين خارجَي التخطيط:

1. **الهيرو `Featured News` (141:15396) صندوق فارغ** `390×668` — مثل خانة المميّز في الفعاليات: الإطار يحجز ولا يصمّم. ونحن نصيّر 991 (شريط الصورة + كتلة البطاقتين الثانويتين في التدفّق). الفرق **323**. ولا يمكن اشتقاق التركيب الداخلي للموبايل من إطار بلا أبناء، فلم يُخترَع: تُرك الهيرو كما هو ويُصرَّح بالفارق.
2. **الإطار نفسه غير متّسق بـ8**: مُذيّله عند `y=2163` بارتفاع 882، أي `3045` مقابل لوحة `3037`. فاللوحة أقصر من محتواها بثمانية بكسل.

والدليل على أن الأرقام المحدَّدة صحيحة: `668 + 47 + 48 + 24 + 1315.5 + 60.5 = 2163` — أي **موضع المُذيّل في الإطار بالضبط**.

و`323 + 8 = 331` = المتبقّي المقاس.

## 13. `artist-detail-mobile`: الكاروسيل الثالث، وسؤالان مفتوحان

`141:16217` (لوحة `390×3658`). من **+1648** إلى **+598**.

ما حُسم:

| الكتلة | Figma | المقاس |
|---|---|---|
| شريط الهيرو (`141:16220`) | `390×688` عند y=−10 ⇒ 0..678 | 678 |
| شريط المسيرة (`Section` 141:16468) | 671.16 | **672** |
| بطاقات الإصدارات | 269 عرضاً، خطوة 289، أوّلها عند 89.98 | 269، خطوة 289، أوّلها عند **90** |

وشريط المسيرة يتفكّك حسابياً بالضبط: `96 + 52 + 24 + 37.67 + 40 + 325.49 + 96 = 671.16`. والحشو 96 في العرضين معاً، والمحتوى يبدأ على بعد **31** من بداية السطر لا 20 (صفّ التبويبات عند `99.67..359` وصفّ الألبومات عند `30..359`).

وهذا **ثالث كاروسيل أفقي RTL** في الملف بعد الفنانين وشريطَي التصفية — الإصدارات الأربعة صفّ واحد لا رصّة عمودية، وهذا وحده كان ‎−1300‎.

### ما بقي: سؤالان لا يحسمهما الملف

1. **قسم ختامي بلا مقابل.** نصيّر شريطاً أخيراً (365 على الموبايل، 498 على الديسكتوب) — والإطار الموبايل لا يحمل ما يقابله: شريط المسيرة ينتهي عند 2787.16 والمُذيّل يبدأ عند 2776، فلا فراغ له أصلاً. وبما أن إطار الديسكتوب يحمله وهو مطابق (+1)، فالأرجح نقصٌ في الماكيت لا قرار بحذف نداء الحجز على الموبايل. **يحتاج قراراً.**
2. **عمود الاقتباس والمعرض**: الإطار يرسمه 735 (`141:16057` عند 1315..2050) ونحن نصيّر 1181.

وهذان مع فجوتين (55 تحت الهيرو و66 قبل شريط المسيرة) يستوعبان الـ598 الباقية.

## 14. `booking-mobile`: الإطار نفسه تخطيط ديسكتوب مسحوق في 390

`141:15629` (لوحة `390×3434`). من **−356** إلى **+10** — أي **0.29٪**، داخل عتبة الـ0.5٪ وخارج عتبة الـ8px.

ما حُسم من الإطار مباشرة:

| الكتلة | Figma | المقاس |
|---|---|---|
| شريط الهيرو (`141:15632`) | `390×688` عند y=−10 ⇒ 0..678 | 678 |
| النموذج (`141:15488`) | (13,762) `363×829` | (13,762) `363×841` |
| الشريط الجانبي (`141:15442`) | (50,1598) `288×563.8` | (50,1610) `288×561` |
| المُذيّل (`141:15777`) | 2552 | 2562 |

**الاكتشاف الحاسم:** إطار الـ390 يضع **حقلين في كل صفّ**، لا حقلاً واحداً. `Frame 42` يحمل `الاسم الكامل` (175) و`رقم الهاتف` (175)، و`Frame 43` يحمل `الميزانية` (164) و`البريد` (197). فـ`grid-cols-1` كان خطأً في القراءة لا في التنفيذ، وتصحيحه وحده أغلق معظم الفارق.

### لماذا لا تُغلَق الـ10 الباقية

لأن الإطار لا يصف تخطيطاً متّسقاً يمكن إعادة بنائه — إنه إطار الديسكتوب مضغوطاً على 390 وأبناؤه يفيضون من آبائهم:

- `Frame 43` (`141:15829`) ارتفاعه **55** وابناه ارتفاع كلٍّ منهما 55 بينما محتوى كلٍّ منهما `27.2 + 51.45 = 78.6`. أي أن الصندوق **يقصّ** محتواه بـ23.6.
- `Container` (`141:15522`) عرضه **451** و`Container` (`141:15555`) عرضه **672** — داخل نموذج عرضه 363. و`Paragraph` (`141:15562`) عرضه 507، فنصّه يُرسم سطراً واحداً (15) بينما يلزمه سطران عند العرض الحقيقي 363.
- المجموع المعلن للنموذج 829 بينما آخر أبنائه ينتهي عند **821.96** — سبعة بكسل لا يفسّرها المحتوى.

فالمتبقّي عندنا **+31 في المجموعة الأولى، −43 في الثانية، +31 في كتلة الإقرار** — خطآن متعاكسان مصدرهما هذه الصناديق المقصوصة. إعادة إنتاجها بأمانة تعني رسم حقول تتراكب فوق القسم الذي تحتها، وهو ما لم يُفعَل: بُني الصفّان بحقلين وبالعروض المرسومة، وتُرك الفائض مُصرَّحاً به. **يحتاج قراراً من المصمّم.**

## 15. `academy-mobile`: مطابق تماماً، وصندوق محجوز ثالث

`139:12420` (لوحة `390×3891`). من **−177** إلى **0**.

الشريط يتفكّك حسابياً بالضبط: `490 + 1413 + 737.91 + 15.09 + 353 = 3009` — وهو موضع المُذيّل في الإطار.

| الكتلة | Figma | المقاس |
|---|---|---|
| شريط الهيرو (`139:13053`) | `390×500` عند y=−10 ⇒ 0..**490** | 490 |
| عنوان المسارات (`139:12432`) | (52,733) `286×99` | (52,733) `286×99` |
| حاوية البطاقات (`139:13166`) | (10,863) `362×1008.84` | (10,863) `362×1009` |
| شريط الأعمدة (`139:13444`) | (0,1903) `388×737.91` | 1903، 738 |
| النشرة (`139:13473`) | (−1,2656) `390×353` | 2656، 353 |

ثلاث ملاحظات:

1. **الهيرو 490 لا 678** — هذا الإطار الموبايل الوحيد الذي يخالف ارتفاع الشريط المشترك.
2. **العنوان مُوسَّط على مقاس 286**، لا مُحاذى لبداية السطر كما في إطار الـ1440 (حيث يبدأ على بعد 560). وسطرَاه في صندوق 99، أي رصف 49.5.
3. **`139:13444` صندوق فارغ محجوز** `388×737.91` — ثالث صندوق من نوعه بعد خانة الفعاليات المميّزة (`144:19982`) وهيرو الأخبار (`141:15396`). فالارتفاع أُعيد إنتاجه بـ`min-h` ولم يُخترَع له محتوى داخلي.

وبالمقابل، إطار النشرة يكشف أن `sm:text-[40px]` في العنوان كان مخترعاً: صندوق `Heading 2` ارتفاعه 36 ونصّه عرضه 326 في **الإطارين معاً** — فالقياس واحد عند 390 وعند 1440. وكذلك صفّ النموذج: 12 من الفجوة وزرّ 149 في العرضين، والفارق الوحيد عرض حقل البريد (198 مقابل 491) — وهو ما يعطيه `flex-1` وحده دون أي نقطة توقّف.

## 16. `home-mobile`: كل شريط مطابق بالضبط، واللوحة أقصر من الإطار بـ89

`136:5854` (لوحة `390×6528`). من **+16** إلى **+90** — وهذا **تحسّن لا تراجع**، وهو ما يفضح خطورة الاعتماد على الفارق الكلّي وحده: الـ+16 السابقة كانت حاصل جمع ستة أخطاء كبيرة يُلغي بعضها بعضاً.

| الشريط | Figma | قبل | بعد |
|---|---:|---:|---:|
| الهيرو | 740 | 740 | **740** |
| من نحن | 1001 | 933 | **1001** |
| أصوات تصنع التاريخ | 775 | 555 | **775** |
| يقولون عن أندلسيا | 450 (+30 فاصل) | 597 | **450** |
| نكتب كي لا تضيع التفاصيل | 1586 | 1570 | **1586** |
| نلتقي في المكان | 726 | 848 | **726** |
| الشريط الختامي | 427.99 | 420 | **428** |
| المُذيّل يبدأ عند | **5735** | 5662 | **5735** |

أي أن **كل شريط يطابق الإطار، والمُذيّل يبدأ عند موضعه بالضبط**. والـ90 المتبقّية هي الإطار نفسه: `5735 + 882 = 6617` مقابل لوحة معلنة `6528`. وهي نفس علّة `news-mobile` (‏+8) بمقدار أكبر.

### أربعة أشرطة أُعيد بناؤها لا تعديلها

1. **أصوات تصنع التاريخ لا تحمل شريطاً أفقياً على الموبايل** بل **شبكة 2×2** من أربع بطاقات `160×239`، عرضها 336 مُوسَّطة، 16 أفقياً و29 رأسياً. والبطاقتان الخامسة والسادسة تُسقَطان: الإطار يرسم أربعاً، وصفّ خامس ببطاقة واحدة اختراع. وإيقاع الشريط `52 + 48 + 38 + (239+29+239) + 38 + 48 + 44 = 775`.
2. **يقولون عن أندلسيا تُسقِط أثاث الكاروسيل كلّه** — لا أسهم ولا نقاط ترقيم: عنوان في صندوق 48 على بعد 83، ثم بطاقة بيضاء واحدة `305×167`. وداخلها ينقلب الترتيب: كتلة الاسم والنجوم والصورة أولاً (36 ارتفاعاً)، والاقتباس تحتها في صندوق 78.
3. **نلتقي في المكان تفكّ الانقسام**: العنوان، ثم الصورة **في التدفّق** `369.735×224`، ثم عمود 326.489. وصفوفها تنقلب أيضاً: شارة التاريخ تُرسم على اليسار الفيزيائي والشارة النوعية على اليمين — أي `flex-row-reverse` في العربية — وتسقط سهم النهاية. ونداء الفعاليات هناك **رابط نصّي 24 ارتفاعاً عند بداية السطر**، لا الزرّ الممتلئ 48 الذي يرسمه إطار الـ1440.
4. **`136:5151` صندوق فارغ محجوز** `401.73×427.99` — رابع صندوق من نوعه. وهو مقصور على إطار الرئيسية: صفحة الفنان تستعمل الشريط نفسه ولا يرسم إطارها الموبايل ما يقابله (‏§13)، فالحجز موضوع على غلاف الصفحة لا على المكوّن.

### ملاحظة على `display: contents`

شريط الفعاليات يرسم الصورة **بين** العنوان والصفوف، بينما الصورة في الشجرة شقيقة للوحة التي تضمّ الاثنين. فبدل نقل العنوان خارج عموده — وهو ما كان سيمسّ تموضع الـ1440 — تُحوَّل اللوحة وعمودها إلى `display: contents` تحت lg، فيصير العنوان والصفوف والنداء أشقّاء للصورة في عمود flex واحد تُرتَّب بـ`order`. ومن lg فصاعداً يعودان صندوقين ولا يتغيّر شيء في إطار الـ1440 (‏قِيس: 0).

### ونتيجة لذلك سقطت ثلاثة مقاسات مخترعة

`sm:text-4xl`/`sm:text-5xl` في عناوين الرئيسية كانت درجات لا يقابلها إطار: عناوين الـ390 كلّها **32** في صندوق 48 (أصوات 222 عرضاً، يقولون 220، نكتب 351)، وعنوان من نحن **61/91.5** — أي مقاس إطار الـ1440 نفسه بلا تدرّج. وكذلك `sm:h-80` في صورة الفعاليات و`sm:grid-cols-2` في شبكة نكتب.
